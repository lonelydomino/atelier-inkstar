---
description: Newsletter section hidden — remind Milo to re-add before/at site launch
alwaysApply: true
---

# Newsletter (temporarily removed)

**Stardust Dispatch** mailing list UI is **hidden** from the homepage until a real provider is wired.

## Remind Milo to re-add when

- Deploying `atelierinkstar.com` to Vercel, or
- Milo picks a newsletter provider (Kit, Buttondown, Resend, etc.)

## How to restore

1. Import `NewsletterSection` in `apps/marketing/src/app/page.tsx`
2. Render `<NewsletterSection />` after `<ChizuruSection />` (or before footer)
3. Wire form to provider — do **not** ship the fake local-only submit on a live site
4. Component still lives at `apps/marketing/src/components/sections/newsletter-section.tsx`

Until then, leave the section off the page.
